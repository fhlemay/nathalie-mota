<div class="grid grid-cols-1 md:grid-cols-2 gap-5 md:mt-12 md:mb-28">

    {% for photo in photos %}
    {{ include( '/components/photo-card.twig', {'photo': photo} ) }}
    {% endfor %}

    {{ include( '/components/lightbox.twig' ) }}

</div>
