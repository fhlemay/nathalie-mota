<!doctype html>
<html>

{% include '/components/html-head.twig' %}

<body x-data="{ 'contactModalOpened': false }" class="{{ body_class }} max-w-[1440px] w-full mx-auto">

    {{ function('wp_body_open') }}

    {% include '/components/header.twig' %}

    {# Set the content width #}
    <div class="mx-auto md:w-4/5">

        <main>
            {% block content %}
            Sorry, no content
            {% endblock %}
        </main>

    </div>
    {% include '/components/footer.twig' %}
</body>

</html>
